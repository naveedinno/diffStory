# diffStory UI kit — direction 3b "Signal / Thread-Ledger"
Six app screens recreated from the codebase + screenshots, elevated to the chosen direction.
Screens: WorkspacePicker, ChangeScope, ReviewHistory, AllFilesDiff, CodeWalkthrough (+ SystemStates). The Atlas map overview was cut — the walkthrough filmstrip is the story overview.
index.html is an interactive click-through: pick repo → scope → read (story/all files) → history.
All styling via tokens in ../../styles.css; components from the compiled bundle namespace.
