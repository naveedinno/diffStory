node_modules/
*.log
.DS_Store

# dist/ is committed so the macOS app and source checkout have a ready local
# server build. Rebuild before committing source changes: npm run build

# diffStory working files live alongside the repo being reviewed.
# Keep them local by default — they describe a single review session.
.diffstory/
.superpowers/
.macos-build/
macos/DiffStoryApp/target/
macos/DiffStoryApp/gen/
macos/DiffStoryApp/icons/icon.png
eval/results/
